requests>=2.31
beautifulsoup4>=4.12
PyYAML>=6.0

# Опционально: нужен, только если включите render: true в config.yaml
# (страница отдаёт счётчики через JS, обычный запрос их не видит)
# playwright>=1.40
