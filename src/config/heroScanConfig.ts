/**
 * Настройки анимации «сканирования» первого экрана (Hero).
 * enabled — общий выключатель; delay — пауза перед запуском в мс.
 */
export const heroScanConfig = {
  enabled: true,
  delay: 3000,
};
